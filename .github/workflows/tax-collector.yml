# Legacy Tax Collector Workflow - DEPRECATED in favor of Rainmaker Orchestrator Judges
# This file is intentionally disabled to 'remove the rest' from the system.

name: Tax Collector (Legacy)
on:
  workflow_dispatch: # Manual only to prevent interference

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated and replaced by the Rainmaker Orchestrator Judges Engine."
