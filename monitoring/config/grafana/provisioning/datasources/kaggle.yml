apiVersion: 1
datasources:
  - name: Kaggle
    type: yesoreyeram-infinity-datasource
    access: proxy
    url: https://www.kaggle.com/api/v1
    jsonData:
      httpHeaderName1: "Authorization"
    secureJsonData:
      httpHeaderValue1: "${KAGGLE_API_TOKEN}"
